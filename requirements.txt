# Streamlit Community Cloud installs from this file.
# The dashboard reads committed JSON only; it needs no Alpaca or model keys,
# and imports nothing beyond the standard library and Streamlit itself.
streamlit>=1.40,<2
